# extide-cli

```sh
$ java -jar extide-1.0.0.jar [OPTIONS] <ARGUMENTS...>
OPTIONS
    -h, --help       print this message.
ARGUMENTS
    Java source files, class files, jar files, war files, and directories.
```
